pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        mavenLocal()
    }
    plugins {
        id "io.quarkus" version "${quarkusPlatformVersion}"
    }
}

rootProject.name = 'website-quarkus'
